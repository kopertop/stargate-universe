# Stargate Universe

Use tabs for indentation.

Prefer functional programming.

Keep code DRY.

Use `$godogen` to generate or update this Godot game from a natural language description.

This is an existing Godot 4.6 GDScript-first project. Preserve the current GDScript codebase, scenes, tests, and project settings unless the user explicitly asks for a fresh Godogen scaffold, C#/.NET migration, or wholesale rebuild.

For targeted Godot engine API questions, use `$godot-api`. Prefer its GDScript reference for this repo unless the task explicitly touches C#.
