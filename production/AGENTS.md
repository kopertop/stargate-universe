# production/

Sprint history and planning notes. `sprints/archive-browser-stack/` (Vite era) and `sprints/sprint-005*` (Godot era) are
closed history from before the Three.js rebuild — do not resume them. `next-development-plan.md` is the forward queue;
`stage.txt` is the one-word project stage. Convert relative dates to absolute when writing notes.
