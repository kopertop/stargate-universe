# Mint test fixtures

| File | Purpose |
|---|---|
| `bad_finger_idle.glb` (+ texture) | Over-bound Idle host from the 2026-07-17 studio spaghetti incident. Kept as a reference for bind tooling / `MintHandGrip.audit_finger_host`. Play-path distortion is guarded by `tests/smoke/mint_loco_combat_pose.gd` on the shipping Eli host. |

Do not use this GLB as a shipping character host.
